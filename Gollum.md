1. Prerequisites
Gollum is a Ruby gem that requires Ruby ≥ 2.4, Git, and several build dependencies. 
`
Ubuntu / Debian:
bash
sudo apt update
sudo apt install -y ruby ruby-dev make zlib1g-dev libicu-dev build-essential git cmake libssl-dev pkg-config`